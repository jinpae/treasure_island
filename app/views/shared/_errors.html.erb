<% if object.errors.any? %>
	<div class="error-messages-wrapper">
		<h2><%= pluralize object.errors.count, "error" %> occurred while handling your request:</h2>

		<ul>
			<% object.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
